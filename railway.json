{
  "projects": [
    {
      "name": "backend",
      "directory": "backend",
      "startCommand": "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    },
    {
      "name": "frontend", 
      "directory": "frontend",
      "startCommand": "npx serve -s build -l $PORT"
    }
  ]
}